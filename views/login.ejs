<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Login - CANTEEN</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <!-- Google Font -->
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&display=swap" rel="stylesheet" />

  <style>
    body {
      font-family: 'Inter', sans-serif;
    }
  </style>
</head>
<body class="min-h-screen flex items-center justify-center">

  <div class="flex w-full max-w-5xl bg-white rounded-xl overflow-hidden">
    <!-- Left Side Image -->
    <div class="hidden lg:block w-1/2 relative">
      <img src="images/item1.png" alt="Login" class="w-full h-full object-cover " />
      <div class="absolute inset-0 bg-black bg-opacity-50 flex flex-col justify-center items-center text-white text-center px-6">
        <h2 class="text-3xl font-bold mb-4"><i class="fas fa-utensils text-red-500"></i> CANTEEN</h2>
        <p class="text-lg">Quick. Fresh. More Time With Your Meal .</p>
      </div>
    </div>

    <!-- Right Side Login Form -->
    <div class="w-full lg:w-1/2 p-8 sm:p-12">
      <h2 class="text-2xl font-bold text-gray-800 mb-6 flex items-center gap-2">
        <i class="fas fa-sign-in-alt text-red-500"></i> Welcome Back!
      </h2>
      <p class="text-gray-600 mb-6">Login to your account to continue spending more time enjoying  your food not in the queue.</p>

      <form method="POST" action="/login" class="space-y-5">
        <!-- Email -->
        <div>
          <label class="block mb-1 text-sm font-medium text-gray-700">Email</label>
          <div class="flex items-center border border-gray-300 rounded px-3 py-2">
            <i class="fas fa-envelope text-gray-400 mr-2"></i>
            <input type="email" name="email" required class="w-full outline-none text-sm" placeholder="you@example.com" />
          </div>
        </div>

        <!-- Password -->
        <div>
          <label class="block mb-1 text-sm font-medium text-gray-700">Password</label>
          <div class="flex items-center border border-gray-300 rounded px-3 py-2">
            <i class="fas fa-lock text-gray-400 mr-2"></i>
            <input type="password" name="password" required class="w-full outline-none text-sm" placeholder="••••••••" />
          </div>
        </div>

        <!-- Forgot + Admin -->
        <div class="flex justify-between text-sm">
          <a href="/forgot" class="text-blue-600 hover:underline">Forgot Password?</a>
          <a href="/admin/login" class="text-gray-600 hover:underline">Login as Admin</a>
        </div>
      <span id="login-error" class="text-red-600 text-sm block">
        <%= error %>
      </span>
        <!-- Submit -->
        <button type="submit" class="w-full bg-red-600 text-white py-2 rounded hover:bg-red-700 transition flex justify-center items-center gap-2">
          <i class="fas fa-sign-in-alt"></i> Login
        </button>
      </form>
    </div>
  </div>

</body>
</html>
