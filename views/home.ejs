<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Home - Canteen</title>
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Inter', sans-serif;
    }
  </style>
</head>
<body class="bg-white text-black">

<!-- Navbar -->
<header class="w-full bg-white shadow-md sticky top-0 z-50">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex items-center justify-between h-20">
      <!-- Logo -->
      <a href="/home" class="flex items-center gap-2">
        <img src="/images/logoCanteen.png" alt="Logo" class="h-12 object-contain">
      </a>

      <!-- Navigation -->
      <nav class="flex gap-6 items-center text-sm font-medium text-gray-700">
        <a href="/home" class="hover:text-red-600">Home</a>
        <a href="/orders" class="hover:text-red-600">My Orders</a>
        <a href="/track" class="hover:text-red-600">Track</a>
        <a href="/cart" class="hover:text-red-600 flex items-center gap-1">
          <i class="fas fa-shopping-cart"></i> Cart (<%= cartCount %>)
        </a>
        <a href="/logout" class="text-red-600 hover:text-red-800 flex items-center gap-1">
          <i class="fas fa-sign-out-alt"></i> Logout
        </a>
      </nav>
    </div>
  </div>
</header>

<!-- Main Content -->
<main class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
  <h2 class="text-3xl font-bold text-gray-800 mb-6">🍔 Available Items</h2>

  <% if (items.length > 0) { %>
    <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-6">
      <% items.forEach(item => { %>
        <div class="bg-white border border-gray-200 rounded-lg shadow hover:shadow-md transition p-4 flex flex-col">
          <div class="flex justify-center mb-4">
            <img src="<%= item.image %>" alt="<%= item.name %>" class="h-32 w-32 object-contain" />
          </div>
          <h3 class="text-lg font-semibold text-gray-800"><%= item.name %></h3>
          <p class="text-sm text-gray-600 h-16 mt-1 overflow-hidden"><%= item.description %></p>
          <p class="text-red-600 font-bold mt-2">R <%= item.price %></p>

          <!-- Action buttons -->
          <div class="mt-auto pt-4 flex justify-between gap-2">
            <form action="/add-to-cart" method="POST">
              <input type="hidden" name="itemId" value="<%= item._id %>">
              <button type="submit" class="text-gray-700 border px-3 py-1 rounded hover:bg-gray-100 text-sm">
                <i class="fas fa-cart-plus"></i> Add to Cart
              </button>
            </form>
            <form action="/buy-now" method="POST">
              <input type="hidden" name="itemId" value="<%= item._id %>">
              <button type="submit" class="bg-red-600 text-white px-3 py-1 rounded hover:bg-red-700 text-sm">
                <i class="fas fa-bolt"></i> Buy Now
              </button>
            </form>
          </div>
        </div>
      <% }); %>
    </div>
  <% } else { %>
    <p class="text-center text-red-500 mt-8">No items available at the moment. Please check back later.</p>
  <% } %>
</main>

</body>
</html>

